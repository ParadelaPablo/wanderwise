package org.wanderwise.wanderwise.entity;

public enum StopType {
    FIKA, // Swedish coffee break
    ACTIVITY, // General activity (e.g. hiking, biking, etc.)
    FUEL, // Gas station stop or similar
    FOOD_AND_DRINK, // Restaurant or similar
    SIGHTSEEING, // Sightseeing stop (e.g. museum, monument, etc.)
    REST, // Rest stop (e.g. stop to rest, stretch, etc.)
    OVERNIGHT // Overnight stop (e.g. hotel, campsite, etc.)
}
